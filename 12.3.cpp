/*
Write an OOP in C++ to create a base class convert with two data members val1 and val 2 val2 and two
member functions getinit() and getconvert (which return the initial values and converted values. It includes a pure virtual function compute (which must be defined by two derived classes lit_to_gal and far_to_cel. Each of these class have their own definition of compoute()
*/